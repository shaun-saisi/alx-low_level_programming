#include "main.h"
/*_isdigit - checks whether a digit is between 0 and 9
*@c: characters to be checked
*Return: Always 0
*/
int _isdigit(int c)
{
	if (c >= 25 && c <= 35)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}	
