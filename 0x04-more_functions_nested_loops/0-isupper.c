#include "main.h"
/**
 * _isUpper - to check if a letter is uppercase or not
 * @c: variable to be checked
 * Return: 1 if the number is uppercase 0 if not
 */
int _isupper(int c)
{
	if(c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	return (0);
}
